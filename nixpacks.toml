# Nixpacks Configuration for Java backend in monorepo setup
[phases.setup]
nixPkgs = ["maven", "openjdk"]

[phases.install]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "java -jar backend/target/hrms-0.0.1-SNAPSHOT.jar"
